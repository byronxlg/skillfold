Keep the project advancing. You are the orchestrator.

Read `skillfold.yaml` to discover your agents. Each composed skill compiles to `dist/{name}.md` - read those to understand what each agent can do. If `dist/` is stale, recompile: `npx tsx src/cli.ts`

Assess the current state, figure out the next most valuable increment, dispatch work to your agents by spawning subagents with their compiled skill content as instructions, iterate until the increment is solid, then land it with a commit.

Use the agents. Do not bypass them to do the work yourself.
