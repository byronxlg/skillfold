{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/byronxlg/skillfold/raw/main/skillfold.schema.json",
  "title": "Skillfold Manifest",
  "description": "Declares the skills and rules a project uses. Run \"skillfold install\" to install them into .claude/skills and .claude/rules and pin exact versions in skillfold.lock.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "skills": {
      "description": "Skills to install, keyed by skill name (the directory name under the skills directory).",
      "type": "object",
      "propertyNames": {
        "pattern": "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$",
        "maxLength": 64
      },
      "additionalProperties": {
        "oneOf": [
          {
            "type": "string",
            "description": "Source string: a local path (./skills/my-skill), github:owner/repo/path@ref, or npm:package/skill-name@version (@installed follows the installed package version).",
            "minLength": 1
          },
          {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "source"
            ],
            "properties": {
              "source": {
                "type": "string",
                "description": "Source without a version pin.",
                "minLength": 1
              },
              "version": {
                "type": [
                  "string",
                  "number"
                ],
                "description": "Version pin: a git tag, branch, or commit SHA for GitHub sources; an exact version, dist-tag, or \"installed\" (the version the project has installed) for npm sources."
              },
              "targets": {
                "type": "array",
                "items": {
                  "enum": [
                    "claude",
                    "codex",
                    "cursor"
                  ]
                },
                "minItems": 1,
                "uniqueItems": true,
                "description": "Subset of top-level targets for this skill. Omitted: all enabled targets."
              }
            }
          }
        ]
      }
    },
    "compose": {
      "description": "Generated skills whose body is the concatenation of other skills' bodies, in declared order.",
      "type": "object",
      "propertyNames": {
        "pattern": "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$",
        "maxLength": 64
      },
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "use"
        ],
        "properties": {
          "description": {
            "type": "string",
            "description": "Frontmatter description of the generated skill."
          },
          "use": {
            "type": "array",
            "description": "Names of skills (or other composed skills) to concatenate, in order.",
            "items": {
              "type": "string"
            },
            "minItems": 1,
            "uniqueItems": true
          },
          "allowed-tools": {
            "description": "allowed-tools frontmatter of the generated skill: a comma-separated string or a list of tool names. Default: the union of the used skills' allowed-tools when every one declares a list; omitted (unrestricted) otherwise.",
            "oneOf": [
              {
                "type": "string",
                "minLength": 1
              },
              {
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1
                },
                "minItems": 1
              }
            ]
          },
          "targets": {
            "type": "array",
            "items": {
              "enum": [
                "claude",
                "codex",
                "cursor"
              ]
            },
            "minItems": 1,
            "uniqueItems": true,
            "description": "Subset of top-level targets for this skill. Omitted: all enabled targets."
          }
        }
      }
    },
    "skillsDir": {
      "type": "string",
      "description": "Directory to install skills into, relative to the manifest. Default: .claude/skills",
      "minLength": 1
    },
    "rules": {
      "description": "Rules to install as <rulesDir>/<name>.md, keyed by rule name. Each source points at a single markdown file (local path, github:, or npm:).",
      "type": "object",
      "propertyNames": {
        "pattern": "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$",
        "maxLength": 64
      },
      "additionalProperties": {
        "oneOf": [
          {
            "type": "string",
            "minLength": 1
          },
          {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "source"
            ],
            "properties": {
              "source": {
                "type": "string",
                "minLength": 1
              },
              "version": {
                "type": [
                  "string",
                  "number"
                ]
              },
              "targets": {
                "type": "array",
                "items": {
                  "enum": [
                    "claude",
                    "codex",
                    "cursor"
                  ]
                },
                "minItems": 1,
                "uniqueItems": true
              },
              "hosts": {
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1
                },
                "minItems": 1,
                "uniqueItems": true
              }
            }
          }
        ]
      }
    },
    "rulesDir": {
      "type": "string",
      "description": "Directory to install rules into, relative to the manifest. Default: .claude/rules",
      "minLength": 1
    },
    "targets": {
      "description": "Tools to install for. Default: [claude]. claude installs to .claude/skills and .claude/rules; codex installs to .agents/skills and a managed AGENTS.md block; cursor installs to .cursor/skills and .cursor/rules.",
      "type": "array",
      "items": {
        "enum": [
          "claude",
          "codex",
          "cursor"
        ]
      },
      "minItems": 1,
      "uniqueItems": true
    }
  }
}
