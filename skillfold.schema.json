{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/byronxlg/skillfold/raw/main/skillfold.schema.json",
  "title": "Skillfold Manifest",
  "description": "Declares the skills a project uses. Run \"skillfold install\" to install them into .claude/skills and pin exact versions in skillfold.lock.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "skills": {
      "description": "Skills to install, keyed by skill name (the directory name under the skills directory).",
      "type": "object",
      "propertyNames": {
        "pattern": "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$",
        "maxLength": 64
      },
      "additionalProperties": {
        "oneOf": [
          {
            "type": "string",
            "description": "Source string: a local path (./skills/my-skill), github:owner/repo/path@ref, or npm:package/skill-name@version.",
            "minLength": 1
          },
          {
            "type": "object",
            "additionalProperties": false,
            "required": ["source"],
            "properties": {
              "source": {
                "type": "string",
                "description": "Source without a version pin.",
                "minLength": 1
              },
              "version": {
                "type": ["string", "number"],
                "description": "Version pin: a git tag, branch, or commit SHA for GitHub sources; an exact version or dist-tag for npm sources."
              }
            }
          }
        ]
      }
    },
    "compose": {
      "description": "Generated skills whose body is the concatenation of other skills' bodies, in declared order.",
      "type": "object",
      "propertyNames": {
        "pattern": "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$",
        "maxLength": 64
      },
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": ["use"],
        "properties": {
          "description": {
            "type": "string",
            "description": "Frontmatter description of the generated skill."
          },
          "use": {
            "type": "array",
            "description": "Names of skills (or other composed skills) to concatenate, in order.",
            "items": { "type": "string" },
            "minItems": 1,
            "uniqueItems": true
          }
        }
      }
    },
    "skillsDir": {
      "type": "string",
      "description": "Directory to install skills into, relative to the manifest. Default: .claude/skills",
      "minLength": 1
    }
  }
}
